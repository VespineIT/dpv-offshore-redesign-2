/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/dpv-offshore-redesign-2',
  assetPrefix: '/dpv-offshore-redesign-2/',
  images: {
    unoptimized: true,
  },
}

module.exports = nextConfig